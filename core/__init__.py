"""
core — Pacote com a lógica de visão computacional do projeto.

Este arquivo (mesmo vazio) é o que diz pro Python "esta pasta é um pacote".
Sem ele, `from core.leitor_video import LeitorVideo` não funcionaria.

Módulos disponíveis:
    leitor_video       — abstração sobre cv2.VideoCapture
    detector_classico  — pipeline OpenCV (MOG2 + morfologia + contornos)
    tracker            — Centroid Tracker implementado do zero
    contador           — contagem por cruzamento de linha
"""
